spring:
  application:
    name: api

  datasource:
    url: jdbc:mysql://localhost:3306/api?createDatabaseIfNotExist=true
    username: root
    password: m0933266
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none

  flyway:
    enabled: true